import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	palette: {
		primary: {
			main: "#00BF2B",
		},
		mode: "dark",
		secondary: {
			main: "#fff",
		},
		success: {
			main: "#00BF2B",
		},
		error: {
			main: "#f10000",
		},
	},
});
