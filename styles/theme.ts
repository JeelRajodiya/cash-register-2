import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	palette: {
		primary: {
			main: "#00BF2B",
		},
		// mode: "dark",
		secondary: {
			main: "#fff",
		},
		success: {
			main: "#00C9C8",
		},
		error: {
			main: "#b0004f",
		},
	},
});
