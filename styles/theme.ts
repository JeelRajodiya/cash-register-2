import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	palette: {
		primary: {
			main: "#050A30",
		},
		// mode: "dark",
		secondary: {
			main: "#fff",
		},
		// success: {
		// 	main: "#1874EC",
		// },
		// error: {
		// 	main: "#b0004f",
		// },
	},
});
