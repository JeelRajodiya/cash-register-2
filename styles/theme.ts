import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	palette: {
		primary: {
			main: "#fcba03",
		},
		mode: "dark",
		secondary: {
			main: "#fff",
		},
		success: {
			main: "#00ff00",
		},
		error: {
			main: "#ff0000",
		},
	},
});
